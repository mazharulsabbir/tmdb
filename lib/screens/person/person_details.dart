import 'package:flutter/material.dart';

class PeopleDetailsScreen extends StatelessWidget {
  const PeopleDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('People'),
      ),
    );
  }
}
